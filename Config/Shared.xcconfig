// Public build settings. Override these in Config/Local.xcconfig.

XAVIER_DEVELOPMENT_TEAM =
XAVIER_BUNDLE_ID = com.example.xavier
XAVIER_APP_GROUP_IDENTIFIER = group.$(XAVIER_BUNDLE_ID)
XAVIER_KEYCHAIN_GROUP_SUFFIX = $(XAVIER_BUNDLE_ID).shared
XAVIER_APP_PROXY_UUID = AA113482-C47E-4326-9633-31C8D2BAE8A1
XAVIER_TARGET_BUNDLE_SUFFIX =

#include? "Local.xcconfig"

DEVELOPMENT_TEAM = $(XAVIER_DEVELOPMENT_TEAM)
PRODUCT_BUNDLE_IDENTIFIER = $(XAVIER_BUNDLE_ID)$(XAVIER_TARGET_BUNDLE_SUFFIX)
